<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :ingredient_id, placeholder: "Ingredient...",collection: Ingredient.all %>
  <%= f.input :description, placeholder: "Quantity" %>
  <%= f.submit "Add Mixers", class: "btn btn-primary" %>
<% end %>
